import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['lh3.googleusercontent.com', 'cdn.dummyjson.com', 'upload.wikimedia.org', 'i.gifer.com'],
  },
};

export default nextConfig;
